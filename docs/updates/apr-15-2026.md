#### Update Report ⸬ Wed Apr 15, 2026  15:02:00 pm - +08:00
![Tests Pass](https://img.shields.io/badge/tests-226%20passing-brightgreen)

---
## **Assistant Catalog**  
[`tests/assistant-catalog.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/assistant-catalog.test.ts)  

- linkifies category and product mentions  
- supports pluralized category mentions  
- builds runtime catalog context with relevant products  
- falls back to defaults when config is missing  
- keeps explicit runtime flags and instructions  

---
## **Cart Computations**  
[`tests/cart-computations.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/cart-computations.test.ts)  

- computeSubtotal  
  - returns 0 for empty cart  
  - computes single item: priceCents * quantity  
  - sums multiple items  
  - uses denomination when no priceByDenomination  
  - uses priceByDenomination when available  
  - accepts custom getUnitPrice for testability  
- computeTax  
  - returns 0 when tax config is null  
  - returns 0 when tax is inactive  
  - computes tax when active  
  - rounds tax to nearest cent  
  - handles missing taxRatePercent as 0  
- computeShipping  
  - returns 0 when subtotal >= minimum (free shipping)  
  - returns 0 when subtotal > minimum  
  - returns shipping fee when subtotal < minimum  
  - returns shipping fee for zero subtotal  
- computeOrderTotals  
  - computes all totals correctly  
  - applies shipping when below minimum  
  - uses defaults when shippingConfig is null  
- getOrderRedirectPath  
  - cards -> /lobby/order/:id/cards  
  - cash_app -> /lobby/order/:id/cashapp  
  - crypto_transfer -> /lobby/order/:id/send  
  - crypto_commerce -> /lobby/order/:id/crypto  
  - crypto-payment -> /lobby/order/:id/crypto  
  - unknown method defaults to /send  
  - coerces paymentMethod to string  
- mapCartItemsToRewardsItems  
  - maps to category  
  - uses Uncategorized when categorySlug is missing  
  - maps multiple items  
- computeEstimatedPoints  
  - returns null when not authenticated  
  - returns null when nextVisitMultiplier is null  
  - returns null when nextVisitMultiplier is undefined  
  - computes points: (subtotal/100) * multiplier  
  - rounds to nearest integer  
- computeCartRewards  
  - returns currentTier based on subtotal  
  - Starter tier for low subtotal  
  - isFirstOrder affects free shipping threshold  
  - bundle bonus when multiple categories  

---
## **Cart Price**  
[`tests/cart-price.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/cart-price.test.ts)  

- uses sale denomination price when product is on sale  
- ignores sale denomination price when sale flag is off  
- ignores sale price when it is not lower than regular price  
- can compute effective and regular bundle totals separately  

---
## **Checkout Shipping**  
[`tests/checkout-shipping.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/checkout-shipping.test.ts)  

- resolveRewardShippingCostDollars  
  - returns free shipping for qualifying first orders  
  - preserves the configured tier shipping cost for returning buyers  
- resolveOrderShippingCents  
  - keeps a free rewards tier free instead of falling back to 1299  
  - uses the rewards tier shipping cost when it is non-zero  
  - falls back to legacy shipping rules when no rewards tier shipping is available  

---
## **Coupon Errors**  
[`tests/coupon-errors.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/coupon-errors.test.ts)  

- extracts the message from structured coupon errors  
- ignores unrelated Convex errors  
- ignores standard errors  

---
## **Coupon Rules**  
[`tests/coupon-rules.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/coupon-rules.test.ts)  

- normalizeCouponCode  
  - trims whitespace and uppercases the code  
- isCouponActive  
  - returns false for disabled, scheduled, or expired coupons  
  - returns true for an enabled coupon within its active window  
- getCouponDiscountCents  
  - applies percentage discounts to subtotal only  
  - caps fixed discounts at the subtotal  
  - respects maximumDiscountCents when present  
- getCouponEligibilityError  
  - defaults to one use per user when no per-user limit is configured  
  - allows reuse until the explicit per-user limit is reached  
  - rejects orders below the minimum subtotal  
  - rejects coupons that have hit their total usage limit  

---
## **Deals Bundle Debug**  
[`tests/deals-bundle-debug.test.tsx`](https://github.com/phtn/unlicensed/blob/main/tests/deals-bundle-debug.test.tsx)  

- renders collapsible details  
- shows pipeline counts when expanded  
- shows per-product filter reasons when expanded  

---
## **Deals Pending Section**  
[`tests/deals-pending-section.test.tsx`](https://github.com/phtn/unlicensed/blob/main/tests/deals-pending-section.test.tsx)  

- renders nothing when outside PendingDealsProvider  
- renders nothing when pendingDeals is empty  
- renders section when pending deals exist  

---
## **Deals Search Params**  
[`tests/deals-searchParams.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/deals-searchParams.test.ts)  

- parseSelectionsString  
  - returns empty map for empty string  
  - returns empty map for whitespace-only string  
  - parses single product:id  
  - parses multiple products  
  - skips malformed parts (missing quantity)  
  - skips invalid quantity (NaN)  
  - skips zero or negative quantity  
  - trims product ids  
- serializeSelections  
  - returns empty string for empty map  
  - serializes single entry  
  - serializes multiple entries  
  - filters out entries with quantity <= 0  
  - round-trip: parse then serialize preserves data  
  - round-trip: serialize then parse preserves data  
- getParamKeysForDealId  
  - has keys for all bundle types  
  - variation keys are unique per bundle  
  - selection keys are unique per bundle  

---
## **Deals Stepper**  
[`tests/deals-stepper.test.tsx`](https://github.com/phtn/unlicensed/blob/main/tests/deals-stepper.test.tsx)  

- renders current value  
- has accessible decrement button  
- has accessible increment button  
- calls onIncrement when increment pressed  
- calls onDecrement when decrement pressed  
- decrement disabled when value at min  
- increment disabled when value at max  
- increment disabled when isComplete  
- respects custom min  

---
## **Deals Types**  
[`tests/deals-types.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/deals-types.test.ts)  

- dealDocToBundleConfig  
  - converts Convex deal doc to BundleConfig  
  - preserves optional fields  
- DEFAULT_BUILD_YOUR_OWN_OZ_CONFIG  
  - has required BundleConfig shape  
  - variations have required BundleVariation shape  
  - has two variations (1/8 and 1/4)  
  - first variation is 8x1/8 oz  
  - has maxPerStrain 2 and lowStockThreshold 3  
- legacy deal ids  
  - LEGACY_DEAL_IDS are non-empty strings  

---
## **Fiat Conversion**  
[`tests/fiat-conversion.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/fiat-conversion.test.ts)  

- converts correctly after swapping USD and EUR  
- converts correctly in both directions for the selected currencies  

---
## **Inventory Adjustments**  
[`tests/inventory-adjustments.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/inventory-adjustments.test.ts)  

- applyInventoryUpdate  
  - adds restock quantities on top of the current quantity  
  - sets exact quantities for manual overrides  
- applyInventoryDeduction  
  - subtracts deductions from the current quantity  
  - clamps deductions at zero  
- buildInventoryInputs  
  - prefers configured available denominations over extra stock map keys  
  - respects the current form-selected denominations when provided  
  - falls back to stocked denomination keys for legacy by-denomination products  

---
## **Low Stock Alerts**  
[`tests/low-stock-alerts.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/low-stock-alerts.test.ts)  

- normalizes and deduplicates recipients  
- schedules once when stock falls below threshold and alerts are configured  
- does not reschedule while an active alert is already open  
- clears the active state after stock recovers above threshold  

---
## **Order Crypto Route**  
[`tests/order-crypto-route.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/order-crypto-route.test.ts)  

- accepts crypto payment methods for /lobby/order/[orderId]/crypto  
- rejects non-crypto payment methods for /lobby/order/[orderId]/crypto  
- uses cards fallback route when payment method is cards  
- uses cash app fallback route when payment method is cash_app  
- defaults fallback route to send for unknown methods  
- marks payment success state only when status is completed  

---
## **Paygate Cards Checkout**  
[`tests/paygate-cards-checkout.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/paygate-cards-checkout.test.ts)  

- returns 400 when orderId is missing  
- returns 409 when order is already paid  
- returns 400 when requested provider is not pre-selected  
- initializes card checkout using predefined account values (no wallet API call)  
- normalizes double-encoded address_in to single encoding in payment URL  
- returns 400 when account has no addressIn  
- legacy paygate checkout route stays as a compatibility alias  

---
## **Paygate Webhook**  
[`tests/paygate-webhook.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/paygate-webhook.test.ts)  

- marks pay with crypto as completed and delegates email delivery to Convex  
- does not change settlement behavior for later completed callbacks  
- does not change settlement behavior for non-crypto methods  

---
## **Payment Email Amount**  
[`tests/payment-email-amount.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/payment-email-amount.test.ts)  

- uses totalWithCryptoFeeCents for crypto pending/success emails  
- falls back to totalCents for crypto orders without totalWithCryptoFeeCents  
- uses total plus processing fee for cash app pending/success emails  
- keeps non-crypto success fallback precedence intact  

---
## **Payment Success Email Delivery**  
[`tests/payment-success-email-delivery.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/payment-success-email-delivery.test.ts)  

- accepts both crypto payment methods and cash app for payment success emails  
- treats missing state as retryable  
- creates a pending state with zero attempts  
- does not retry while sending or after sent  
- applies retry backoff for failed sends  
- stops retrying after max attempts  
- queues cash app payment success email when order enters processing with a completed payment  
- does not queue cash app payment success email once it has already been sent  

---
## **Pending Payment Email Delivery**  
[`tests/pending-payment-email-delivery.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/pending-payment-email-delivery.test.ts)  

- treats missing state as retryable  
- creates a pending state with zero attempts  
- does not retry while sending or after sent  
- applies retry backoff for failed sends  

---
## **Processing Fee**  
[`tests/processing-fee.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/processing-fee.test.ts)  

- subtracts discounts from the order total before crypto fees  
- computes crypto fee as the delta from the discounted total  
- applies the default 5% processing fee to cash app orders  
- uses the configured cash app fee when provided  
- resolves a fee-inclusive payable total for cash app orders  
- stores cash app processing fee without populating crypto totals  
- stores crypto-only totals separately from processing fees  

---
## **Product CSV Import Ordering**  
[`tests/product-csv-import-order.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/product-csv-import-order.test.ts)  

- processes replacement rows before create rows while preserving relative order  
- mergeDefinedCsvImportFields only overwrites fields provided by the CSV row  

---
## **Product CSV Import**  
[`tests/product-csv-import.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/product-csv-import.test.ts)  

- includes shared inventory and packaging headers in the export format  
- returns a file error for an empty CSV  
- parses CSV files with a UTF-8 BOM header prefix  
- parses multiline quoted fields without splitting a product into multiple rows  
- parses legacy shared_weight rows and normalizes them to shared inventory  
- parses packaging fields from CSV rows  
- preserves _id values so matching rows can replace existing products  
- treats blank boolean cells on update rows as omitted values  
- still parses explicit false boolean values from CSV rows  
- flags slug conflicts when the row does not own the existing slug  
- prefers replacement rows over create rows when the same slug appears in one import  
- requires master stock fields for shared inventory rows without denomination stock input  
- accepts shared inventory rows when per-denomination stock is provided  
- accepts shared inventory rows when denomination stock is provided via exported stock columns  
- reports numeric field type errors with field-specific messages  
- reports array field type errors with field-specific messages  
- reports invalid enum values with explicit messages  

---
## **Product CSV Upload Helpers**  
[`tests/product-csv-upload-helpers.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/product-csv-upload-helpers.test.ts)  


> *Helpers are utility functions that perform common tasks across other functions or components.*  


- getPreviewColumns hides internal fields and keeps status columns  
- seedDenominationColumnsFromMaps expands JSON denomination maps into preview columns and product fields  
- seedDefaultImage adds the default image when the CSV row is missing one  
- seedDefaultImage does not inject a default image for update rows with a blank image cell  
- seedDefaultDenominationStock  
  - fills default stock for non-shared inventory rows  
  - does not inject default stock for shared inventory rows  
  - does not inject default stock for update rows with blank stock cells  
- buildRowsWithConflicts  
  - derives a slug from the name when the slug cell is blank  
  - appends the category slug when a derived slug already exists  
  - allows same-_id replacements to keep an existing slug  
  - keeps a blank slug untouched for update rows  
  - adds category errors and slug conflicts for invalid replacement targets  
  - flags later create rows that duplicate a slug inside the same import  
- getPreviewCellIssue maps validation messages to required, type, and conflict labels  
- getRowPreviewIssues returns issue entries with the matching columns  
- mapImportRowErrors remaps backend indices to the original CSV row numbers after filtering invalid rows  

---
## **Product Stock**  
[`tests/product-stock.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/product-stock.test.ts)  

- converts ounces to pounds for shared inventory deductions  
- detects shared weight inventory products  
- computes line deductions in the master stock unit  
- uses master stock as the total stock for shared pools  
- uses master stock as the total stock for shared count pools  
- uses shared master stock even when a denomination count is missing  
- treats the smallest denomination as one shared unit when master stock is units  
- scales shared unit consumption by denomination ratio  
- converts shared stock into selectable denomination count using the master stock unit  
- returns shared count inventory in denomination-sized units  
- treats missing by-denomination stock as unavailable  
- preserves by-denomination quantity for direct add-to-cart checks  
- formats shared stock with units for admin/store displays  

---
## **Resend Rate Limit**  
[`tests/resend-rate-limit.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/resend-rate-limit.test.ts)  

- spaces queued sends to the configured interval  

---
## **Rewards**  
[`tests/rewards.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/rewards.test.ts)  

- computeCashBackAmount  
  - calculates cash back from eligible subtotal  
  - clamps negative subtotals to zero  
- computeRewards  
  - uses Bronze tier defaults for low subtotal repeat orders  
  - waives shipping for first orders at or above the first-order threshold  
  - activates bundle bonus when enough unique categories are present  
  - caps progress and removes next tier details at Platinum  
  - supports custom rewards config overrides  
- formatRewardsCurrency  
  - formats dollars as USD currency  

---
## **Send Payment**  
[`tests/send-payment.test.tsx`](https://github.com/phtn/unlicensed/blob/main/tests/send-payment.test.tsx)  

- normalizes hash references and preserves non-hash values  
- builds verification candidates with relay wallets before wallet fallback  
- shows a duplicate hash error and disables submit before verification  
- verifies the transaction, updates payment, and relays once  

---
## **Signup Welcome Email Trigger**  
[`tests/signup-welcome-email-trigger.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/signup-welcome-email-trigger.test.ts)  

- schedules signup activity and welcome email for a newly created user  
- does not reschedule the welcome email for an existing user  

---
## **Table Global Filter**  
[`tests/table-global-filter.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/table-global-filter.test.ts)  

- matches product names by the provided column id  
- normalizes diacritics and whitespace for text matches  
- matches array and object-backed cell values through filter tokens  

---
## **Welcome Email**  
[`tests/welcome-email.test.ts`](https://github.com/phtn/unlicensed/blob/main/tests/welcome-email.test.ts)  

- prefers active first-order or welcome coupons over generic active coupons  
- returns the only active coupon when there is no explicit welcome match  
- returns null when multiple active coupons exist but none look like a welcome coupon  
- ignores disabled, scheduled, and expired coupons  
